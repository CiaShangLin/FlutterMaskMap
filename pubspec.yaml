name: flutter_mask_map
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^7.3.1
  flutter_spinkit: ^5.1.0
  google_maps_flutter: ^2.1.0
  permission_handler: ^8.2.5


dev_dependencies:
  flutter_test:
    sdk: flutter


#  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/mask.json
